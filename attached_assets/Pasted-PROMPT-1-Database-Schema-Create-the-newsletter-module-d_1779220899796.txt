PROMPT 1 — Database Schema
Create the newsletter module database schema. Run drizzle-kit push after and confirm all tables created.
Add the following tables to the schema:
newslettersTable:
export const newslettersTable = pgTable("newsletters", {
  id: serial("id").primaryKey(),
  title: text("title").notNull(),
  slug: text("slug").notNull().unique(),
  topic: text("topic").notNull(),
  visaCategory: text("visa_category").notNull(), // 'EB1A' | 'NIW' | 'O1A' | 'ALL'
  ideaIndex: integer("idea_index"),
  ideaTitle: text("idea_title"),
  ideaSummary: text("idea_summary"),
  contentHtml: text("content_html"),
  contentText: text("content_text"),
  heroImageUrl: text("hero_image_url"),
  heroImageCredit: text("hero_image_credit"),
  heroImageCreditUrl: text("hero_image_credit_url"),
  status: text("status").default("draft"),
  // draft | ideas_generated | idea_selected | content_generated | approved | sending | sent | failed
  generatedIdeasJson: jsonb("generated_ideas_json").default([]),
  externalReferences: jsonb("external_references").default([]),
  wordCount: integer("word_count"),
  createdByStaffId: text("created_by_staff_id"),
  approvedByStaffId: text("approved_by_staff_id"),
  approvedAt: timestamp("approved_at", { withTimezone: true }),
  sentAt: timestamp("sent_at", { withTimezone: true }),
  recipientCount: integer("recipient_count").default(0),
  openCount: integer("open_count").default(0),
  clickCount: integer("click_count").default(0),
  resendBroadcastId: text("resend_broadcast_id"),
  webViewPath: text("web_view_path"),
  createdAt: timestamp("created_at", { withTimezone: true }).defaultNow(),
  updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow(),
});
newsletterUnsubscribesTable:
export const newsletterUnsubscribesTable = pgTable("newsletter_unsubscribes", {
  id: serial("id").primaryKey(),
  email: text("email").notNull().unique(),
  token: text("token").notNull().unique(),
  source: text("source"), // 'client' | 'prospect' | 'lead'
  unsubscribedAt: timestamp("unsubscribed_at", { withTimezone: true }).defaultNow(),
});
Add columns to prospectsTable:
newsletterOptIn: boolean("newsletter_opt_in").default(true),
newsletterUnsubscribeToken: text("newsletter_unsubscribe_token"),
Add columns to profilesTable:
newsletterOptIn: boolean("newsletter_opt_in").default(true),
newsletterUnsubscribeToken: text("newsletter_unsubscribe_token"),
Run drizzle-kit push and confirm all tables and columns created with zero errors.
